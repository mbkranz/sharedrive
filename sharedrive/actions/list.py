""" List all resources available/described to this sharedrive and the status"""

# TODO: list descriptor(s) contents without syncing, and add option to sync only missing/changed resources
# NOTE: this should include all set descriptors
# NOTE: include option to list on certain descriptor(s) only, by drive service, by resource name (with wildcard).
# NOTE: include auth status of each resource (and method of auth if fetched/synced) by default
